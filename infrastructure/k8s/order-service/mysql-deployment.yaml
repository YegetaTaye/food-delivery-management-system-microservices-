apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-db
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-db
  template:
    metadata:
      labels:
        app: order-db
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_DATABASE
              value: order_db
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: order-db-secret
                  key: MYSQL_USER
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: order-db-secret
                  key: MYSQL_PASSWORD
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: order-db-secret
                  key: MYSQL_ROOT_PASSWORD
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
          imagePullPolicy: IfNotPresent
      volumes:
        - name: mysql-data
          hostPath:
            path: /tmp/k3d-order-mysql-data
            type: DirectoryOrCreate
