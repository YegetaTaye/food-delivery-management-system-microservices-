apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: dev
spec:
  selector:
    app: analytics-service
  ports:
    - protocol: TCP
      port: 3007
      targetPort: 3007
  type: ClusterIP
