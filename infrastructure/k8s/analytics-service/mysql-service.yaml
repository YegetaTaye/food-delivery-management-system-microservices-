apiVersion: v1
kind: Service
metadata:
  name: analytics-db
  namespace: dev
spec:
  selector:
    app: analytics-db
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  type: ClusterIP
