apiVersion: v1
kind: Service
metadata:
  name: delivery-db
  namespace: dev
spec:
  selector:
    app: delivery-db
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  type: ClusterIP
