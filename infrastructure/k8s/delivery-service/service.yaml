apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: dev
spec:
  selector:
    app: delivery-service
  ports:
    - protocol: TCP
      port: 3005
      targetPort: 3005
  type: ClusterIP
